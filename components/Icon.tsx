import React from 'react';
import { 
  Scan, 
  History, 
  Settings, 
  User, 
  Sun, 
  Moon, 
  Search, 
  CheckCircle2, 
  ArrowLeft,
  Share,
  ZoomIn,
  ZoomOut,
  Maximize,
  RotateCw,
  Type,
  WrapText,
  SpellCheck,
  Save,
  FileText,
  Copy,
  Trash2,
  MoreVertical,
  ChevronRight,
  FileImage,
  UploadCloud,
  Menu,
  X,
  ClipboardPaste
} from 'lucide-react';

// Using Lucide React for consistent, crisp icons that match the "Senior Engineer" standard
// Mapping string names to components for easier dynamic usage if needed, or exporting directly

export {
    Scan, History, Settings, User, Sun, Moon, Search, CheckCircle2, 
    ArrowLeft, Share, ZoomIn, ZoomOut, Maximize, RotateCw, Type, 
    WrapText, SpellCheck, Save, FileText, Copy, Trash2, MoreVertical,
    ChevronRight, FileImage, UploadCloud, Menu, X, ClipboardPaste
};
